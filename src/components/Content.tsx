import React from 'react';
import styles from './Content.module.scss';

export default () => {
    return (
        <div className={styles.content}>
            <h1>Newest Blogs</h1>
        </div>
    )
}